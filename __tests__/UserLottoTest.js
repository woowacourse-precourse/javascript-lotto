const UserLotto = require("../src/UserLotto");

describe("UserLotto 클래스 테스트", () => {
  // test("lottoSort가 정상 동작한다.", () => {
  //   expect(() => {
  //     new UserLotto(3);
  //   }).toThrow("[ERROR]");
  // });
  // test("getUserLotto 메서드가 정상 동작한다.", () => {
  //   const userLotto = new UserLotto(5);
  //   userLotto.setUserLotto();
  //   const userLottos = lotto.getWonLotto();
  //   expect(userLottos).toEqual();
  // });
});
