const MissionUtils = require("@woowacourse/mission-utils");
const { Console, Random } = MissionUtils;

class App {
  buyLottos() {
    Console.readLine("구입금액을 입력해 주세요.", (inputs) => {});
  }
  play() {}
}

module.exports = App;
