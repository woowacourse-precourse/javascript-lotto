const LOTTO = Object.freeze({
  PRICE: 1000,
  NUM_START: 1,
  NUM_LAST: 45,
  NUM_LENGTH: 6,
});

const PRIZE = Object.freeze({
  FIFTH: 5000,
  FOURTH: 50000,
  THIRD: 1500000,
  SECOND: 30000000,
  FIRST: 2000000000,
});

const RANK = Object.freeze({
  LENGTH: 5,
});

module.exports = {
  LOTTO,
  PRIZE,
  RANK,
};
