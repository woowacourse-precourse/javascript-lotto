const {
  isNumberType,
  isThousandUnits,
  isValuesNumberType,
} = require("./validate");
const {
  inputUserValue,
  printMessage,
  generateRandomNumbers,
} = require("./missionUtils");

module.exports = {
  isNumberType,
  isThousandUnits,
  isValuesNumberType,
  inputUserValue,
  printMessage,
  generateRandomNumbers,
};
