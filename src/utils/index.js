const { isNumberType, isThousandUnits } = require("./validate");
const {
  inputUserValue,
  printMessage,
  generateRandomNumbers,
} = require("./missionUtils");

module.exports = {
  isNumberType,
  isThousandUnits,
  inputUserValue,
  printMessage,
  generateRandomNumbers,
};
