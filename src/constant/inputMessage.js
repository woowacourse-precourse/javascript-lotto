const THOUSAND = 1000;

const GAME_START_MESSAGE = "구입금액을 입력해 주세요.\n";
const BUY_LOTTOS_MESSAGE = (count) => `${count}개를 구매했습니다.`;

const RANGE = {
  START: 1,
  END: 45,
  COUNT: 6,
};

module.exports = {
  GAME_START_MESSAGE,
  BUY_LOTTOS_MESSAGE,
  THOUSAND,
  RANGE,
};
