const ISNUMBER = '[ERROR] 숫자를 입력해주세요';
const ISNUMBERDIVIDED = (SHARE) => `[ERROR] ${SHARE}로 나누어 떨어져야 합니다`;
const ISNUMBERBIGGER = (STANDARD) => `[ERROR] ${STANDARD}보다는 커야 합니다`;

module.exports = { ISNUMBER, ISNUMBERDIVIDED, ISNUMBERBIGGER };
