const PURCHASE = Object.freeze({
  INPUT: '구입금액을 입력해 주세요.\n',
  UNIT: 1000,
});

const LOTTO = Object.freeze({
  MIN_NUMBER: 1,
  MAX_NUMBER: 45,
  SIZE: 6,
});

module.exports = {
  PURCHASE,
  LOTTO,
};
