const LOTTO = {
  MIN_NUM: 1,
  MAX_NUM: 45,
  NUM: 6,
};

const PROFIT = {
  THREE: 5000,
  FOUR: 50000,
  FIVE: 1500000,
  FIVE_AND_BONUS: 30000000,
  SIX: 2000000000,
};

const REGEX_NUM = /^[0-9]+$/;

const PRICE_PER_LOTTO = 1000;

module.exports = {
  LOTTO,
  PROFIT,
  REGEX_NUM,
  PRICE_PER_LOTTO,
};
