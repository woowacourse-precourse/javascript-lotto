const LOTTO = Object.freeze({
  PRICE: 1000,
  START_NUMBER: 1,
  END_NUMBER: 45,
  NUMBER_OF_DIGITS: 6,
});

const SYSTEM = Object.freeze({
  JOIN_CHARACTER: ',',
});

module.exports = { LOTTO, SYSTEM };
