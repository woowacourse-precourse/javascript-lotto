const Console = require('@woowacourse/mission-utils');

class GetNumber {
    toWin(numbers) {
        numbers.split(',');
        console.log(numbers);
    };

    bonus(number) {
        console.log(number);
    }
}

module.exports = GetNumber;

