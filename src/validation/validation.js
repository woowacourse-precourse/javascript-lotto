const { amountValidation } = require('./amountValidation');
const { winningValidation } = require('./winningValidation');
const { bonusValidation } = require('./bonusValidation');

module.exports = { amountValidation, winningValidation, bonusValidation };
